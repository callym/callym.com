---
title: rebuilding callym.com
categories: web text text2 another category blah blah2 blah3
date: 2016-09-20
---
The original callym.com was built using [Jekyll](http://jekyllrb.com),
a static blog generator that I had to hack around to use as a portfolio site.

```javascript
if (exploit_drive_point) {
	multithreading_widget.controllerUddi += dll;
} else {
	nosql_card = boot(lcdVirtualization(designTypeGolden,
			chip_install_retina, graymail_analog_core), 648832 -
			rippingDithering, 709134);
	motherboardPptpMemory(unmountOspfPublishing);
	barcraftAdslPartition(dtdYottabyte - ediTiff, tiff_hoc, query);
}
if (kilobitClass(power_cms - storage_compression_tag, errorDown - 40,
		fileVfat(thyristor_speakers_signature, recursion_surface)) ==
		soap_mask_standby) {
	dhcp_lun_access += analog_menu;
	boxHostIrq = 320626;
	kibibyteIcio *= led;
}
displayMemoryStorage = bitrate;
```

```rust
// This code is editable and runnable!
fn main() {
    // A simple integer calculator:
    // `+` or `-` means add or subtract by 1
    // `*` or `/` means multiply or divide by 2

    let program = "+ + * - /";
    let mut accumulator = 0;

    for token in program.chars() {
        match token {
            '+' => accumulator += 1,
            '-' => accumulator -= 1,
            '*' => accumulator *= 2,
            '/' => accumulator /= 2,
            _ => { /* ignore everything else */ }
        }
    }

    println!("The program \"{}\" calculates the value {}",
              program, accumulator);
}
```