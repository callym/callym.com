{% extends "layouts/page.nunjucks" %}

{% block contents %}
{% set class = "center" %}
<div class="sublinks row-flex">
    <a href="{{ rootPath }}about" class="{{ class }}">
        statement
    </a>
{% for p in about %}
    <a href="{{ rootPath }}{{ p.path }}" class="{{ class }}">
        {{ p.title }}
    </a>
{% endfor %}
</div>

{{ super() }}

{% endblock %}